<h1>Listing works</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Data di Inizio</th>
    <th>Data di Fine</th>
    <th>Luoghi</th>
    <th>Extra</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @works.each do |work| %>
<% keys=work.get_extra_keys %>
  <tr>
    <td><%= work.name %></td>
    <td><%= work.start %></td>
    <td><%= work.end %></td>
    <td><%= keys.length %></td>
    <td><%=  %></td>
    <td><%= link_to 'Show', work %></td>
    <td><%= link_to 'Edit', edit_work_path(work) %></td>
    <td><%= link_to 'Destroy', work, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Project', new_work_path %>
